namespace = tp_prisoner_system

##############################
## TP Prisoner system
##############################

# Prisoners arrives at destination
tp_prisoner_system.0001 = {
	type = character_event
	title = tp_prisoner_system.0100.t
	desc = {
		desc = "pwet"
	}
	theme = prison
	#content_source = mod_tp
	left_portrait = {
		character = scope:recipient
		animation = war_over_tie
		trigger =  { location = root.capital_province }
	}
	right_portrait = {
		character = scope:prisoner_from_sender
		animation = grief
	}
	lower_right_portrait = scope:sender
	#lower_left_portrait = scope:prisoner_from_recipient

	immediate = {
		#root = { save_temporary_scope_as = prisoner_recipient }
		## For recipient's prisoner
		#
		#

		scope:prisoner_from_sender.imprisoner = {
			trigger_event = { id = tp_prisoner_system.0002 }
		}
	}

	option = {
		name = {
			text = tp_prisoner_system.0001.a
		}
	}
}

tp_prisoner_system.0002 = {
	type = letter_event
	opening = tp_prisoner_system.0002.opening
	desc = "andito na siya broski"
	sender = scope:recipient

	immediate = {
		scope:recipient = { 
			take_prisoner = { PRISONER = scope:prisoner_from_sender } 
		}
	}

	option = {
		name = {
			text = "pwet"
		}
	}
}