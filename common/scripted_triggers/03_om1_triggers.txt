##############################
## GM1 scripted_triggers
##############################

# This thing.
gm1_mod_trigger = {
	trigger_if = {
		limit = { has_global_variable = gm1_is_loaded }
		always = yes
	}
}

# Rescue and Vengeance
mod_trigger = {
	trigger_if = {
		trigger_if = {
			limit = { has_global_variable = rv_is_loaded }
			always = yes
		}
	}
}